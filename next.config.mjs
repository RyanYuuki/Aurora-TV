/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
      domains: ['sup-proxy.zephex0-f6c.workers.dev'], // Array of allowed domains
    },
  };
  
  export default nextConfig;
  